package com.example.testexam

fun main() {

}

fun strings(strings: List<String>) {
    var largestNumber = strings[0]
    // sorting by length
    strings.forEach { string ->
        if(largestNumber.length > string.length) {

        }
    }
}